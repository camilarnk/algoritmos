Algoritmo "8_lista1_volpir"

Var
    length, high, vpd: real

Inicio
    escreva("Comprimento da pirâmide: ")
    leia(length)
    escreva("Altura da pirâmide: ")
    leia(high)
    vpd <- (length * length) * (high / 3)
    escreva("Volume da pirâmide:",vpd)

Fimalgoritmo