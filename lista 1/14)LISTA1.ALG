Algoritmo "14)lista1"

Var
    num1, num2, result: real

Inicio
    escreva("Primeiro número: ")
    leia(num1)
    escreva("Segundo número: ")
    leia(num2)
    result <- (num1-num2)/10
    escreval("A décima parte da subtração de", num1, " por", num2, " é:", result)

Fimalgoritmo